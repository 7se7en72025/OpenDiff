services:
  - type: web
    name: opendiff-backend
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "5000"
      - key: MONGODB_URI
        sync: false
